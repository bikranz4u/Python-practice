#You can use if statements to run code if a certain condition holds.
#If an expression evaluates to True, some statements are carried out. Otherwise,won't.
#Python uses indentation (white space at the beginning of a line) to delimit blocks of code.
#Indentation is mandatory; programs won't work without it. 
#colon(:) at the end of the expression in the if statement

if 10 > 5:
    print ("This is True")
print ("program execution reached End")